import { default as getAllTransactions } from './getAllTransactions';
import { default as addTransactions } from './addTransactions';
import { default as removeTransactions } from './removeTransactions';

export default {
  getAllTransactions,
  addTransactions,
  removeTransactions,
};
